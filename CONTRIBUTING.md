# 贡献指南

首先，感谢你愿意为射箭赛事积分统计系统做出贡献！ 🎯

## 行为准则

本项目遵循 [贡献者公约](https://www.contributor-covenant.org/)。参与本项目即表示你同意遵守其条款。

## 如何贡献

### 报告 Bug

如果你发现了 Bug，请通过 [Issues](https://github.com/your-repo/issues) 提交：

1. **使用清晰的标题**描述问题
2. **详细描述复现步骤**
3. **提供预期行为**和**实际行为**
4. **包含相关的日志和截图**
5. **说明你的环境信息**（操作系统、Docker 版本等）

### 建议新功能

如果你有新功能的想法：

1. 在 Issues 中查看是否已有类似建议
2. 创建新 Issue，标记为 `enhancement`
3. 详细描述功能的用途和预期效果
4. 说明为什么这个功能对项目有价值

### 提交代码

#### 准备工作

1. **Fork 项目**到你的 GitHub 账户
2. **克隆 Fork 的仓库**到本地
   ```bash
   git clone https://github.com/your-username/archery-scoring-system.git
   cd archery-scoring-system
   ```
3. **添加上游仓库**
   ```bash
   git remote add upstream https://github.com/original-repo/archery-scoring-system.git
   ```
4. **创建新分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

#### 开发流程

1. **启动开发环境**
   ```bash
   docker compose up -d
   ```

2. **进行你的修改**
   - 遵循代码规范（见下文）
   - 编写清晰的代码
   - 添加必要的注释
   - 编写或更新测试

3. **测试你的更改**
   ```bash
   # 后端测试
   docker compose exec backend pytest
   
   # 前端测试
   docker compose exec frontend npm run test
   ```

4. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   ```

5. **保持分支最新**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

6. **推送到你的 Fork**
   ```bash
   git push origin feature/your-feature-name
   ```

7. **创建 Pull Request**
   - 在 GitHub 上从你的分支创建 PR 到主仓库的 `main` 分支
   - 填写 PR 模板
   - 关联相关的 Issue

## 代码规范

### Python (后端)

- **遵循 PEP 8** 代码风格
- **使用类型注解**
  ```python
  def get_user(user_id: int) -> Optional[User]:
      pass
  ```
- **编写 docstring**
  ```python
  def calculate_score(arrows: List[int]) -> int:
      """
      计算箭矢总分
      
      Args:
          arrows: 箭矢得分列表
          
      Returns:
          总分
      """
      return sum(arrows)
  ```
- **最大行长度**: 100 字符
- **导入顺序**: 标准库 → 第三方库 → 本地模块

### JavaScript/Vue (前端)

- **使用 ES6+ 语法**
- **Composition API** 优先于 Options API
- **组件命名**: PascalCase
  ```vue
  <script setup>
  import MyComponent from './MyComponent.vue'
  </script>
  ```
- **Props 定义完整**
  ```javascript
  const props = defineProps({
    title: {
      type: String,
      required: true
    }
  })
  ```
- **使用 const/let**，避免 var
- **使用模板字符串**

### Git 提交规范

使用 [约定式提交](https://www.conventionalcommits.org/zh-hans/)：

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的脚注]
```

**类型：**
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动
- `ci`: CI/CD 配置

**示例：**
```
feat(auth): 添加用户登录功能

实现了基于 JWT 的用户认证：
- 登录 API 端点
- Token 验证中间件
- 用户信息获取

Closes #123
```

## Pull Request 指南

### PR 标题

- 使用约定式提交格式
- 清晰描述变更内容
- 示例：`feat: 添加运动员管理功能`

### PR 描述

**必须包含：**

1. **变更说明**
   - 做了什么更改
   - 为什么要做这个更改
   - 如何解决问题

2. **测试**
   - 已执行的测试
   - 如何验证功能

3. **截图**（如适用）
   - UI 变更的前后对比
   - 功能演示

4. **关联 Issue**
   - 使用 `Closes #123` 或 `Fixes #456`

### PR 检查清单

提交 PR 前确认：

- [ ] 代码遵循项目规范
- [ ] 已添加/更新测试
- [ ] 所有测试通过
- [ ] 已更新相关文档
- [ ] 提交信息清晰明确
- [ ] 已解决所有冲突
- [ ] PR 描述完整

## 代码审查

### 审查者职责

- 检查代码质量和规范
- 验证功能正确性
- 提供建设性反馈
- 及时回复和批准

### 被审查者职责

- 积极响应反馈
- 解释设计决策
- 及时修改代码
- 保持礼貌和专业

## 开发环境

### 必需工具

- Docker >= 20.10
- Docker Compose >= 2.0
- Git

### 推荐工具

- **编辑器**: VS Code, PyCharm, WebStorm
- **VS Code 插件**:
  - Volar (Vue)
  - Python
  - Pylance
  - ESLint
  - Prettier

### 环境设置

1. 克隆项目
2. 启动服务：`./start.sh`
3. 访问：
   - 前端: http://localhost:8080
   - 后端: http://localhost:8000
   - API 文档: http://localhost:8000/docs

## 文档

如果你的更改需要更新文档：

- **README.md**: 项目介绍和快速开始
- **DEVELOPMENT.md**: 开发指南
- **API 文档**: 使用 FastAPI 自动生成
- **代码注释**: 复杂逻辑需要注释

## 测试

### 后端测试

```bash
# 运行所有测试
docker compose exec backend pytest

# 运行特定测试
docker compose exec backend pytest tests/test_api.py

# 查看覆盖率
docker compose exec backend pytest --cov=app
```

### 前端测试

```bash
# 运行测试
docker compose exec frontend npm run test

# 运行 E2E 测试
docker compose exec frontend npm run test:e2e
```

## 发布流程

发布由维护者负责：

1. 更新 CHANGELOG.md
2. 更新版本号
3. 创建 Git tag
4. 发布 Release notes

## 获取帮助

如有问题：

- 📖 查看 [文档](./README.md)
- 💬 在 Issue 中提问
- 📧 联系维护者

## 许可证

贡献的代码将采用与项目相同的 [MIT 许可证](./LICENSE)。

---

再次感谢你的贡献！ 🙏
