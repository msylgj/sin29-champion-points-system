# 实现计划 - 射箭赛事积分统计系统

**项目状态**: 🚀 开发进行中  
**最后更新**: 2026-01-30  

## 📋 整体任务清单

### Phase 1: 核心API接口 (优先级: 🔴 最高)

#### 1.1 运动员管理 API
- [ ] **POST** `/api/athletes` - 创建运动员
- [ ] **GET** `/api/athletes` - 列表查询（支持分页、筛选）
- [ ] **GET** `/api/athletes/{id}` - 获取单个运动员详情
- [ ] **PUT** `/api/athletes/{id}` - 更新运动员信息
- [ ] **DELETE** `/api/athletes/{id}` - 删除运动员
- [ ] **GET** `/api/athletes/{id}/scores` - 获取运动员所有成绩

#### 1.2 成绩管理 API
- [ ] **POST** `/api/scores` - 录入单条成绩
- [ ] **POST** `/api/scores/batch` - 批量导入成绩
- [ ] **GET** `/api/scores` - 查询成绩（支持年度、季度、赛制等多维筛选）
- [ ] **PUT** `/api/scores/{id}` - 修改成绩
- [ ] **DELETE** `/api/scores/{id}` - 删除成绩
- [ ] **GET** `/api/scores/{id}` - 获取单条成绩详情

#### 1.3 赛事管理 API
- [ ] **POST** `/api/events` - 创建赛事
- [ ] **GET** `/api/events` - 列表查询
- [ ] **GET** `/api/events/{id}` - 获取赛事详情
- [ ] **PUT** `/api/events/{id}` - 更新赛事
- [ ] **DELETE** `/api/events/{id}` - 删除赛事
- [ ] **GET** `/api/events/{id}/scores` - 获取赛事的所有成绩

#### 1.4 积分统计 API
- [ ] **GET** `/api/stats/rankings` - 获取排名列表（年度、季度维度）
- [ ] **GET** `/api/stats/athlete/{id}/aggregate` - 获取运动员积分汇总
- [ ] **GET** `/api/stats/top-performers` - 获取绩效最优者
- [ ] **POST** `/api/stats/recalculate` - 重新计算积分（管理功能）

#### 1.5 积分规则管理 API
- [ ] **GET** `/api/scoring-rules` - 获取规则列表
- [ ] **GET** `/api/scoring-rules/{id}` - 获取规则详情
- [ ] **POST** `/api/scoring-rules` - 创建规则
- [ ] **PUT** `/api/scoring-rules/{id}` - 更新规则

#### 1.6 数据导入导出 API
- [ ] **POST** `/api/import/athletes` - 导入运动员（支持Excel、CSV）
- [ ] **POST** `/api/import/scores` - 导入成绩
- [ ] **GET** `/api/export/athletes` - 导出运动员数据
- [ ] **GET** `/api/export/scores` - 导出成绩数据
- [ ] **GET** `/api/export/rankings` - 导出排名数据

### Phase 2: 数据模型与数据库 (优先级: 🔴 最高)

#### 2.1 数据库迁移
- [ ] 确认所有表已创建（athletes, scores, events, event_participants, athlete_aggregate_points, scoring_rules, operation_logs）
- [ ] 验证索引都已建立
- [ ] 创建Alembic迁移脚本

#### 2.2 数据模型完善
- [ ] 确认 Athlete 模型
- [ ] 确认 Score 模型
- [ ] 确认 Event 模型
- [ ] 确认 EventParticipant 模型
- [ ] 确认 AthleteAggregatePoints 模型
- [ ] 确认 ScoringRule 模型
- [ ] 确认 OperationLog 模型

#### 2.3 Pydantic Schema
- [ ] AthleteSchema (Create, Read, Update, List)
- [ ] ScoreSchema (Create, Read, Update, List, BatchImport)
- [ ] EventSchema (Create, Read, Update, List)
- [ ] EventParticipantSchema (Create, Read, Update, List)
- [ ] AggregatePointsSchema (Read)
- [ ] ScoringRuleSchema (Create, Read, Update)
- [ ] RankingSchema (Read)

### Phase 3: 业务逻辑服务 (优先级: 🟠 高)

#### 3.1 运动员服务
- [ ] 创建运动员
- [ ] 批量导入运动员
- [ ] 查询和搜索运动员
- [ ] 更新运动员信息
- [ ] 删除运动员（及相关数据）

#### 3.2 成绩服务
- [ ] 录入单条成绩
- [ ] 批量导入成绩（Excel）
- [ ] 成绩验证与去重
- [ ] 成绩编辑与删除
- [ ] 成绩有效性标记

#### 3.3 赛事服务
- [ ] 创建赛事
- [ ] 查询赛事
- [ ] 获取赛事统计信息
- [ ] 赛事状态管理

#### 3.4 积分计算服务 (扩展existing ScoringCalculator)
- [ ] 完成排名赛积分计算（已基本完成，需验证）
- [ ] 完成淘汰赛积分计算（已基本完成，需验证）
- [ ] 完成团体赛积分计算（已基本完成，需验证）
- [ ] 18米减半规则实现
- [ ] 参赛人数系数计算
- [ ] 批量重算积分

#### 3.5 排名统计服务
- [ ] 生成年度排名
- [ ] 生成季度排名
- [ ] 按性别分组排名
- [ ] 按弓种分组排名
- [ ] 积分汇总表更新

#### 3.6 操作日志服务
- [ ] 记录所有数据修改
- [ ] 提供操作历史查询
- [ ] 支持操作审计

### Phase 4: 前端页面 (优先级: 🟠 高)

#### 4.1 页面结构
- [ ] 主仪表板页面（显示统计数据）
- [ ] 运动员管理页面（CRUD + 批量导入）
- [ ] 成绩管理页面（CRUD + 批量导入）
- [ ] 赛事管理页面（CRUD）
- [ ] 排名统计页面（多维展示）
- [ ] 数据导入导出页面
- [ ] 系统管理页面（积分规则、操作日志等）

#### 4.2 功能模块
- [ ] 数据表格组件（搜索、筛选、排序、分页）
- [ ] 批量导入组件（进度显示、错误处理）
- [ ] 数据导出功能
- [ ] 排名展示组件（支持多维筛选）
- [ ] 图表展示（性能趋势等）

#### 4.3 交互功能
- [ ] 表单验证
- [ ] 数据操作确认框
- [ ] 错误处理与提示
- [ ] 加载状态显示
- [ ] 成功/失败消息提示

### Phase 5: 测试与优化 (优先级: 🟡 中)

#### 5.1 单元测试
- [ ] 积分计算逻辑测试
- [ ] 数据验证测试
- [ ] API端点测试

#### 5.2 集成测试
- [ ] API完整流程测试
- [ ] 数据库事务测试

#### 5.3 性能优化
- [ ] 数据库查询优化
- [ ] 缓存策略实现
- [ ] API响应时间优化

#### 5.4 文档完善
- [ ] API接口文档（Swagger）
- [ ] 积分规则详细说明
- [ ] 数据导入导出格式说明
- [ ] 常见问题FAQ

### Phase 6: 部署与运维 (优先级: 🟡 中)

- [ ] Docker打包优化
- [ ] 生产环境配置
- [ ] 数据备份方案
- [ ] 监控和告警配置
- [ ] 性能监控

---

## 🎯 当前阶段：Phase 1 - 核心API接口 (进行中)

### ✅ 已完成的任务

#### 第一批：基础CRUD接口
1. **运动员管理 API** ✅
   - ✅ 创建Pydantic schemas (`backend/app/schemas/athlete.py`)
   - ✅ 实现athlete服务 (`backend/app/services/athlete_service.py`)
   - ✅ 实现athlete路由 (`backend/app/routers/athletes.py`)
   - **API端点**:
     - `POST /api/athletes` - 创建运动员
     - `GET /api/athletes` - 列表查询
     - `GET /api/athletes/{id}` - 获取详情
     - `PUT /api/athletes/{id}` - 更新信息
     - `DELETE /api/athletes/{id}` - 删除运动员
     - `POST /api/athletes/batch/import` - 批量导入

2. **成绩管理 API** ✅
   - ✅ 创建Pydantic schemas (`backend/app/schemas/score.py`)
   - ✅ 实现score服务 (`backend/app/services/score_service.py`)
   - ✅ 实现score路由 (`backend/app/routers/scores.py`)
   - ✅ 积分计算集成 (使用 ScoringCalculator)
   - **API端点**:
     - `POST /api/scores` - 录入成绩
     - `GET /api/scores` - 查询成绩
     - `GET /api/scores/{id}` - 获取单条
     - `PUT /api/scores/{id}` - 修改成绩
     - `DELETE /api/scores/{id}` - 删除成绩
     - `POST /api/scores/batch/import` - 批量导入
     - `POST /api/scores/recalculate` - 重新计算
     - `GET /api/scores/athlete/{id}/scores` - 运动员成绩

3. **赛事管理 API** ✅
   - ✅ 创建Pydantic schemas (`backend/app/schemas/event.py`)
   - ✅ 实现event路由 (`backend/app/routers/events.py`)
   - **API端点**:
     - `POST /api/events` - 创建赛事
     - `GET /api/events` - 列表查询
     - `GET /api/events/{id}` - 获取详情
     - `PUT /api/events/{id}` - 更新赛事
     - `DELETE /api/events/{id}` - 删除赛事

#### 第二批：统计API ✅
4. **排名统计 API** ✅
   - ✅ 创建Pydantic schemas (`backend/app/schemas/aggregate_points.py`)
   - ✅ 实现stats路由 (`backend/app/routers/stats.py`)
   - **API端点**:
     - `GET /api/stats/rankings` - 获取排名列表
     - `GET /api/stats/athlete/{id}/aggregate` - 积分汇总
     - `GET /api/stats/top-performers` - 绩效最优者

### 📚 已生成的文档
- ✅ `IMPLEMENTATION_PLAN.md` - 整体实现计划
- ✅ `API_DOCUMENTATION.md` - 详细API接口文档

### 🔄 代码结构确认

已创建的文件:
```
backend/app/
├── schemas/
│   ├── __init__.py
│   ├── athlete.py          ✅ 运动员Schema
│   ├── score.py            ✅ 成绩Schema
│   ├── event.py            ✅ 赛事Schema
│   └── aggregate_points.py  ✅ 积分汇总Schema
├── routers/
│   ├── __init__.py (已更新)
│   ├── health.py           (现有)
│   ├── athletes.py         ✅ 运动员路由
│   ├── scores.py           ✅ 成绩路由
│   ├── events.py           ✅ 赛事路由
│   └── stats.py            ✅ 统计路由
└── services/
    ├── __init__.py (已更新)
    ├── scoring_calculator.py (现有)
    ├── athlete_service.py   ✅ 运动员服务
    └── score_service.py     ✅ 成绩服务
```

### 🧪 验证状态

- ✅ Python 语法检查通过 (`python3 -m py_compile`)
- ✅ 所有routers/*.py 通过编译
- ✅ 所有services/*.py 通过编译
- ✅ 所有schemas/*.py 通过编译
- ⏳ 数据库初始化脚本验证 (需要启动Docker)
- ⏳ API端点集成测试 (需要启动服务)

### 📋 立即开始的任务（本次sprint）

## 📝 代码结构规划

### 新增文件清单

```
backend/app/
├── schemas/
│   ├── athlete.py          # 运动员Schema
│   ├── score.py            # 成绩Schema
│   ├── event.py            # 赛事Schema
│   ├── event_participant.py # 参与者Schema
│   ├── aggregate_points.py  # 积分汇总Schema
│   └── scoring_rule.py      # 积分规则Schema
├── routers/
│   ├── athletes.py         # 运动员路由
│   ├── scores.py           # 成绩路由
│   ├── events.py           # 赛事路由
│   ├── stats.py            # 统计路由
│   ├── import_export.py     # 导入导出路由
│   └── scoring_rules.py     # 积分规则路由
├── services/
│   ├── athlete_service.py   # 运动员服务
│   ├── score_service.py     # 成绩服务
│   ├── event_service.py     # 赛事服务
│   ├── stats_service.py     # 统计服务
│   ├── import_service.py    # 导入服务
│   └── export_service.py    # 导出服务
└── utils/
    ├── validators.py       # 数据验证工具
    ├── excel_helper.py      # Excel处理工具
    └── error_handler.py     # 错误处理

frontend/src/
├── pages/
│   ├── DashboardPage.vue    # 仪表板
│   ├── AthletesPage.vue     # 运动员管理
│   ├── ScoresPage.vue       # 成绩管理
│   ├── EventsPage.vue       # 赛事管理
│   ├── RankingsPage.vue     # 排名统计
│   └── SystemPage.vue       # 系统管理
├── api/
│   ├── athlete.js
│   ├── score.js
│   ├── event.js
│   ├── stats.js
│   └── import-export.js
└── components/
    ├── DataTable.vue        # 数据表格
    ├── ImportDialog.vue     # 导入对话框
    ├── RankingChart.vue     # 排名图表
    └── StatsCard.vue        # 统计卡片
```

---

## ⏱️ 时间估计

- **Phase 1 (API接口)**: 8-10小时
- **Phase 2 (数据模型)**: 2-3小时（已80%完成）
- **Phase 3 (业务逻辑)**: 6-8小时
- **Phase 4 (前端)**: 10-12小时
- **Phase 5 (测试)**: 4-5小时
- **Phase 6 (部署)**: 2-3小时

**总计**: 32-41小时

---

## 🔥 快速启动

现在开始实现 Phase 1.1 - 运动员管理 API
